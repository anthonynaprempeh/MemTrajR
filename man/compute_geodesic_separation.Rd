% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{compute_geodesic_separation}
\alias{compute_geodesic_separation}
\title{Compute Frame-by-Frame Geodesic Separation Between Two Domains}
\usage{
compute_geodesic_separation(x1, y1, z1, x2, y2, z2, R)
}
\arguments{
\item{x1, y1, z1}{Numeric vectors. 3D coordinates of domain 1 (micrometres).}

\item{x2, y2, z2}{Numeric vectors. 3D coordinates of domain 2 (micrometres).}

\item{R}{Positive numeric. Vesicle radius in micrometres.}
}
\value{
Numeric vector of geodesic separations (micrometres), one value
per frame.
}
\description{
Given 3D coordinates of two domains for each frame, computes the
geodesic (great-circle arc) distance between them on a sphere of
radius \code{R}.
}
\examples{
R  <- 8
n  <- 50
x1 <- rnorm(n, sd = 2); y1 <- rnorm(n, sd = 2); z1 <- sqrt(R^2 - x1^2 - y1^2)
x2 <- rnorm(n, sd = 2); y2 <- rnorm(n, sd = 2); z2 <- sqrt(R^2 - x2^2 - y2^2)
d  <- compute_geodesic_separation(x1, y1, z1, x2, y2, z2, R = R)
summary(d)
}
