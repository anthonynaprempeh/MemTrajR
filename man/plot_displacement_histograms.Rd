% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msd.R
\name{plot_displacement_histograms}
\alias{plot_displacement_histograms}
\title{Plot 1D Displacement Histograms (Delta x, Delta y, Delta z)}
\usage{
plot_displacement_histograms(
  relx,
  rely,
  z,
  lag,
  n_bins,
  frame_interval,
  domain_name
)
}
\arguments{
\item{relx}{Numeric vector of relative x coordinates (micrometres).}

\item{rely}{Numeric vector of relative y coordinates (micrometres).}

\item{z}{Numeric vector of z coordinates on the sphere (micrometres).}

\item{lag}{Positive integer. Lag in frames.}

\item{n_bins}{Positive integer. Number of histogram bins.}

\item{frame_interval}{Positive numeric. Time between frames (seconds).}

\item{domain_name}{Character. Prefix for output file names.}
}
\value{
Invisibly, a list with elements \code{dx}, \code{dy}, \code{dz} (numeric
displacement vectors). Output files are written as a side effect.
}
\description{
For a given lag, computes frame-to-frame displacements in x, y, and z,
saves a three-panel PNG histogram, and writes the displacements to CSV.
}
\examples{
\dontrun{
r    <- 10
relx <- rnorm(200, sd = 0.3)
rely <- rnorm(200, sd = 0.3)
z    <- sqrt(r^2 - relx^2 - rely^2)
plot_displacement_histograms(relx, rely, z,
  lag = 2, n_bins = 20,
  frame_interval = 0.1, domain_name = "domain1")
}
}
