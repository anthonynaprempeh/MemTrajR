% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{bin_separations}
\alias{bin_separations}
\title{Bin Geodesic Separations and Return Frame-Level Data}
\usage{
bin_separations(d_geo, n_bins = 20, frame_interval)
}
\arguments{
\item{d_geo}{Numeric vector of geodesic separations (micrometres),
as returned by \code{\link[=compute_geodesic_separation]{compute_geodesic_separation()}}.}

\item{n_bins}{Positive integer. Number of equal-width bins spanning
the range of \code{d_geo}. Default \code{20}.}

\item{frame_interval}{Positive numeric. Time between frames (seconds).}
}
\value{
A list with:
\describe{
\item{frame_bins}{Data frame with columns \code{frame}, \code{time_s},
\code{separation_um}, and \code{bin}.}
\item{bin_counts}{Data frame with columns \code{bin} and
\code{count_frames}.}
\item{edges}{Numeric vector of bin-edge values.}
\item{bin_midpoints}{Numeric vector of bin midpoint values.}
}
}
\description{
Assigns each frame's geodesic separation to a histogram bin and
returns a data frame suitable for downstream analyses.
}
\examples{
d_geo <- abs(rnorm(200, mean = 5, sd = 1))
result <- bin_separations(d_geo, n_bins = 20, frame_interval = 0.1)
head(result$frame_bins)
result$bin_counts
}
