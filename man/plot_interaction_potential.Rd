% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{plot_interaction_potential}
\alias{plot_interaction_potential}
\title{Plot Effective Interaction Potential}
\usage{
plot_interaction_potential(potential_data, min_separation, sample_name)
}
\arguments{
\item{potential_data}{Data frame as returned by
\code{\link[=estimate_interaction_potential]{estimate_interaction_potential()}}.}

\item{min_separation}{Numeric. Contact distance \eqn{(d_1 + d_2)/2}
in micrometres.}

\item{sample_name}{Character. Used as plot title prefix and output file
prefix.}
}
\value{
Invisibly \code{NULL}. A PNG file is written as a side effect.
}
\description{
Saves a PNG of the interaction potential \eqn{U(r)/k_BT} vs. centre-to-
centre separation, with a reference point marking \eqn{(d_1 + d_2)/2}.
}
\examples{
\dontrun{
d_geo <- abs(rnorm(200, mean = 5, sd = 1))
bins  <- bin_separations(d_geo, n_bins = 15, frame_interval = 0.1)
pot   <- estimate_interaction_potential(bins$bin_counts, bins$bin_midpoints)
plot_interaction_potential(pot, min_separation = 3.14, sample_name = "s01")
}
}
