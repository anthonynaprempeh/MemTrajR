% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msd.R
\name{compute_msd_3d}
\alias{compute_msd_3d}
\title{Compute 3D Geodesic MSD}
\usage{
compute_msd_3d(relx, rely, r, max_lag = NULL)
}
\arguments{
\item{relx}{Numeric vector of relative x coordinates (micrometres).}

\item{rely}{Numeric vector of relative y coordinates (micrometres).}

\item{r}{Positive numeric. Sphere (vesicle) radius in micrometres.}

\item{max_lag}{Integer. Maximum lag to compute. Defaults to
\code{length(relx) - 1}.}
}
\value{
A data frame with columns:
\describe{
\item{lag}{Lag index (integer frames).}
\item{msd}{Mean squared geodesic displacement (micrometres squared).}
}
}
\description{
Computes the mean squared displacement using geodesic (great-circle)
distances on a sphere of radius \code{r}.
}
\examples{
r    <- 10
relx <- rnorm(100, sd = 0.5)
rely <- rnorm(100, sd = 0.5)
# keep points inside sphere
keep <- (relx^2 + rely^2) < r^2
msd  <- compute_msd_3d(relx[keep], rely[keep], r = r, max_lag = 20)
head(msd)
}
