% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msd.R
\name{compute_msd_2d}
\alias{compute_msd_2d}
\title{Compute 2D Euclidean MSD}
\usage{
compute_msd_2d(relx, rely, max_lag = NULL)
}
\arguments{
\item{relx}{Numeric vector of relative x coordinates (micrometres).}

\item{rely}{Numeric vector of relative y coordinates (micrometres).}

\item{max_lag}{Integer. Maximum lag to compute. Defaults to
\code{length(relx) - 1}.}
}
\value{
A data frame with columns:
\describe{
\item{lag}{Lag index (integer frames).}
\item{msd}{Mean squared displacement (micrometres squared).}
}
}
\description{
Computes the mean squared displacement using flat Euclidean distances
for a single domain trajectory.
}
\examples{
relx <- cumsum(rnorm(100, sd = 0.1))
rely <- cumsum(rnorm(100, sd = 0.1))
msd  <- compute_msd_2d(relx, rely, max_lag = 20)
head(msd)
}
