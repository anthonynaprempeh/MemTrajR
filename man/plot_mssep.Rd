% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{plot_mssep}
\alias{plot_mssep}
\title{Plot MSSep on Log-Log Axes}
\usage{
plot_mssep(mssep_data, sample_name, y_min = 0.01, y_max = 10)
}
\arguments{
\item{mssep_data}{Data frame with columns \code{dt} and \code{ms_inc_um2}, as
returned by \code{\link[=compute_mssep]{compute_mssep()}}.}

\item{sample_name}{Character. Used as plot title prefix and output file
prefix.}

\item{y_min}{Positive numeric. Lower y-axis limit. Default \code{0.01}.}

\item{y_max}{Positive numeric. Upper y-axis limit. Default \code{10}.}
}
\value{
Invisibly \code{NULL}. A PNG file is written as a side effect.
}
\description{
Saves a PNG log-log plot of the mean square separation increment vs
lag time.
}
\examples{
\dontrun{
d_geo <- abs(rnorm(300, mean = 5, sd = 0.5))
ms    <- compute_mssep(d_geo, frame_interval = 0.1)
plot_mssep(ms, sample_name = "sample01")
}
}
