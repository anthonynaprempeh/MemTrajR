% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{fit_mssep}
\alias{fit_mssep}
\title{Fit MSSep Curve (Linear and Power-Law)}
\usage{
fit_mssep(mssep_data, fit_until = 1)
}
\arguments{
\item{mssep_data}{Data frame with columns \code{dt} and \code{ms_inc_um2}, as
returned by \code{\link[=compute_mssep]{compute_mssep()}}.}

\item{fit_until}{Positive numeric. Upper time limit (seconds) for the
fitting window. Default \code{1}.}
}
\value{
A named list with elements \code{fit_summary} (data frame),
\code{D_sep} (numeric), \code{linear_slope}, \code{linear_intercept},
\code{power_prefactor}, \code{power_exponent}, and \code{log_intercept}.
}
\description{
Fits the MSSep vs lag-time data with a linear model and a log-log
power-law model over an early-time window, and estimates a separation
diffusion coefficient \eqn{D_{sep} = \text{slope} / 4}.
}
\examples{
d_geo <- abs(rnorm(300, mean = 5, sd = 0.5))
ms    <- compute_mssep(d_geo, frame_interval = 0.1)
fit   <- fit_mssep(ms, fit_until = 1)
fit$fit_summary
}
