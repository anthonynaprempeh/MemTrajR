% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{analyze_separation}
\alias{analyze_separation}
\title{Full Separation and Interaction Potential Analysis}
\usage{
analyze_separation(
  input_file,
  sample_name,
  R,
  d1,
  d2,
  frame_interval,
  fit_until = 1,
  n_bins = 20
)
}
\arguments{
\item{input_file}{Character. Path to a CSV file with columns \code{x1}, \code{y1},
\code{z1}, \code{x2}, \code{y2}, \code{z2} (3D coordinates per frame).}

\item{sample_name}{Character. Prefix for all output file names.}

\item{R}{Positive numeric. Vesicle radius in micrometres.}

\item{d1, d2}{Positive numeric. Diameters of the two domains
(micrometres). Used to compute the contact distance
\eqn{(d_1 + d_2)/2}.}

\item{frame_interval}{Positive numeric. Time between frames (seconds).}

\item{fit_until}{Positive numeric. Upper time limit (seconds) for the
MSSep fitting window. Default \code{1}.}

\item{n_bins}{Positive integer. Number of histogram bins. Default \code{20}.}
}
\value{
Invisibly, a named list with elements \code{d_geo}, \code{frame_bins},
\code{bin_counts}, \code{potential_data}, \code{mssep_data}, and \code{mssep_fit}.
All output files are written as a side effect.
}
\description{
High-level wrapper that reads 3D domain coordinates, computes geodesic
separations, bins them, estimates the interaction potential, computes
MSSep, and writes all outputs (CSVs and PNGs) to the working directory.
}
\examples{
\dontrun{
results <- analyze_separation(
  input_file     = "0004_3d.csv",
  sample_name    = "0004_3d",
  R              = 8.02,
  d1             = 3.14,
  d2             = 3.14,
  frame_interval = 0.1,
  fit_until      = 1,
  n_bins         = 20
)
}
}
