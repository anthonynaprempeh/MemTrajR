% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separation.R
\name{compute_mssep}
\alias{compute_mssep}
\title{Compute Mean Square Separation Increment (MSSep)}
\usage{
compute_mssep(d_geo, frame_interval)
}
\arguments{
\item{d_geo}{Numeric vector of geodesic separations (micrometres).}

\item{frame_interval}{Positive numeric. Time between frames (seconds).}
}
\value{
A data frame with columns \code{dt} (lag time in seconds) and
\code{ms_inc_um2} (mean square separation increment in micrometres squared).
}
\description{
Computes \eqn{\langle [\Delta r(\tau)]^2 \rangle} as a function of
lag time \eqn{\tau} for a geodesic separation time series.
}
\examples{
d_geo <- abs(rnorm(200, mean = 5, sd = 0.5))
ms    <- compute_mssep(d_geo, frame_interval = 0.1)
head(ms)
}
